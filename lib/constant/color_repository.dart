import 'package:flutter/material.dart';

const colorBackGroundPage = Color(0xffFCEDEA);

const colorPrimary =  Color(0xffF6BEB1);
const colorAccent =  Color(0xffFBDEAC);

const colorOfCircularProgress = colorPrimary;
const colorBackOfCircularProgress = colorAccent;

const colorOfTextOfPhoneNumber = Color(0xff828282);

const colorButtonDisable = Color(0xffC3C7CB);
const colorButtonEnable = Color(0xff000000);
const linearGradient = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [Colors.white, colorBackGroundPage]);

const colorOfErrorText = Color(0xffEB5757);
const colorIconBackOfAppBar = Color(0xff243443);

const  senderColorContainer = colorPrimary;
const  receiverColorContainer = colorAccent;
